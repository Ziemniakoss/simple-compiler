fun int sum(int x, int y) {
	return x y +;
}

fun int main() {
	var int firstOperand = 9;
	var real secondOperand = 10;
	var int result = sum(firstOperand, secondOperand);
	writeInt(result);
	writeReal(result);
	var int input = readInt();
	writeInt(input result *);
}
